# Satteri's WASI binding allocates a SharedArrayBuffer at module init,
# which requires the page to be cross-origin isolated.
/*
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp

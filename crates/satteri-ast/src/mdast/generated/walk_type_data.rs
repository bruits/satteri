//! @generated by `cargo run -p satteri-layout-codegen`. Do not edit by hand.

use crate::walk::{write_str16, write_str32};
use satteri_arena::{Arena, Mdast};

/// Serialize a node's `type_data` to the walk wire. `true` if handled here.
#[allow(clippy::manual_range_patterns)]
pub(crate) fn write_mdast_type_data_inline(
    arena: &Arena<Mdast>,
    node_type: u8,
    type_data: &[u8],
    out: &mut Vec<u8>,
) -> bool {
    match node_type {
        2 => {
            out.push(if !type_data.is_empty() { type_data[0] } else { 1 });
        }
        7 | 10 | 13 | 25 | 26 => {
            write_str32(arena, out, type_data, 0);
        }
        8 => {
            write_str16(arena, out, type_data, 0);
            write_str16(arena, out, type_data, 8);
            write_str32(arena, out, type_data, 16);
        }
        9 => {
            write_str16(arena, out, type_data, 0);
            write_str16(arena, out, type_data, 8);
            write_str16(arena, out, type_data, 16);
            write_str16(arena, out, type_data, 24);
        }
        15 => {
            write_str16(arena, out, type_data, 0);
            write_str16(arena, out, type_data, 8);
        }
        16 => {
            write_str16(arena, out, type_data, 0);
            write_str16(arena, out, type_data, 8);
            write_str16(arena, out, type_data, 16);
        }
        17 => {
            write_str16(arena, out, type_data, 0);
            write_str16(arena, out, type_data, 8);
            out.push(if type_data.len() > 16 { type_data[16] } else { 0 });
        }
        18 => {
            write_str16(arena, out, type_data, 0);
            write_str16(arena, out, type_data, 8);
            out.push(if type_data.len() > 16 { type_data[16] } else { 0 });
            write_str16(arena, out, type_data, 20);
        }
        19 => {
            write_str16(arena, out, type_data, 0);
            write_str16(arena, out, type_data, 8);
        }
        20 => {
            write_str16(arena, out, type_data, 0);
            write_str16(arena, out, type_data, 8);
            out.push(if type_data.len() > 16 { type_data[16] } else { 0 });
        }
        27 => {
            write_str16(arena, out, type_data, 0);
            write_str32(arena, out, type_data, 8);
        }
        28 => {
            write_str32(arena, out, type_data, 8);
        }
        102 | 103 | 104 => {
            write_str32(arena, out, type_data, 0);
        }
        _ => return false,
    }
    true
}

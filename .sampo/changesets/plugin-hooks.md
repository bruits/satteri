---
npm/satteri: minor
---

Adds `before`/`after` lifecycle hooks to plugins that run exactly once per document, even an empty one. `ctx.replaceNode` can now swap the document root for a whole new tree.

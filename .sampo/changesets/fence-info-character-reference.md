---
cargo/satteri-pulldown-cmark: patch
cargo/satteri-ast: patch
cargo/satteri-napi: patch
npm/satteri: patch
---

Fixed fenced code block `lang` and `meta` splitting on whitespace that a character reference produced.

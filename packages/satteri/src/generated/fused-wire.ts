// @generated by `cargo run -p satteri-layout-codegen`. Do not edit by hand.

import type { Position } from "unist";
import type { ArenaWire } from "../types.js";
import type { HastProperty } from "../hast/hast-reader.js";
import { decodeElementProp } from "../hast/element-props.js";
import { restorePhantomSpaces } from "../phantom.js";
import { W_START_OFFSET, W_DATA_LEN, W_DATA_OFFSET } from "./arena-layout.js";

const FUSED_ENUM_0 = ["shortcut", "collapsed", "full"] as const;

/** Install `position` (a zero start line marks a synthesized node) and the tag's fixed
 *  fields straight off the wire; `false` hands the tag to the caller's reader path. */
export function readMdastWireNode(
  wire: ArenaWire,
  nodeId: number,
  nodeType: number,
  node: object,
): boolean {
  const { u8, u32, pool } = wire;
  const w = wire.nodesW + nodeId * wire.strideW;
  const wp = w + W_START_OFFSET;
  const startLine = u32[wp + 2] ?? 0;
  if (startLine !== 0) {
    (node as { position?: Position }).position = {
      start: { offset: u32[wp] ?? 0, line: startLine, column: u32[wp + 3] ?? 0 },
      end: { offset: u32[wp + 1] ?? 0, line: u32[wp + 4] ?? 0, column: u32[wp + 5] ?? 0 },
    };
  }
  const n = node as Record<string, unknown>;
  switch (nodeType) {
    case 7:
    case 10:
    case 13:
    case 25:
    case 26:
    {
      const dl = u32[w + W_DATA_LEN] ?? 0;
      const db = (wire.typeDataB + (u32[w + W_DATA_OFFSET] ?? 0)) >> 2;
      const o0 = u32[db] ?? 0;
      n.value = dl >= 8 ? pool.substring(o0, o0 + (u32[db + 1] ?? 0)) : "";
      return true;
    }
    case 2:
    {
      const dl = u32[w + W_DATA_LEN] ?? 0;
      const db = (wire.typeDataB + (u32[w + W_DATA_OFFSET] ?? 0)) >> 2;
      n.depth = dl > 0 ? (u8[(db << 2) + 0] ?? 1) : 1;
      return true;
    }
    case 8:
    {
      const dl = u32[w + W_DATA_LEN] ?? 0;
      const db = (wire.typeDataB + (u32[w + W_DATA_OFFSET] ?? 0)) >> 2;
      const o0 = u32[db] ?? 0;
      const s0 = dl >= 8 ? pool.substring(o0, o0 + (u32[db + 1] ?? 0)) : "";
      n.lang = s0 === "" ? null : s0;
      const o1 = u32[db + 2] ?? 0;
      const s1 = dl >= 16 ? pool.substring(o1, o1 + (u32[db + 3] ?? 0)) : "";
      n.meta = s1 === "" ? null : s1;
      const o2 = u32[db + 4] ?? 0;
      n.value = dl >= 24 ? pool.substring(o2, o2 + (u32[db + 5] ?? 0)) : "";
      return true;
    }
    case 9:
    {
      const dl = u32[w + W_DATA_LEN] ?? 0;
      const db = (wire.typeDataB + (u32[w + W_DATA_OFFSET] ?? 0)) >> 2;
      const o0 = u32[db] ?? 0;
      n.url = dl >= 8 ? pool.substring(o0, o0 + (u32[db + 1] ?? 0)) : "";
      const o1 = u32[db + 2] ?? 0;
      const s1 = dl >= 16 ? pool.substring(o1, o1 + (u32[db + 3] ?? 0)) : "";
      n.title = s1 === "" ? null : s1;
      const o2 = u32[db + 4] ?? 0;
      n.identifier = dl >= 24 ? pool.substring(o2, o2 + (u32[db + 5] ?? 0)) : "";
      const o3 = u32[db + 6] ?? 0;
      n.label = dl >= 32 ? pool.substring(o3, o3 + (u32[db + 7] ?? 0)) : "";
      return true;
    }
    case 15:
    {
      const dl = u32[w + W_DATA_LEN] ?? 0;
      const db = (wire.typeDataB + (u32[w + W_DATA_OFFSET] ?? 0)) >> 2;
      const o0 = u32[db] ?? 0;
      n.url = dl >= 8 ? pool.substring(o0, o0 + (u32[db + 1] ?? 0)) : "";
      const o1 = u32[db + 2] ?? 0;
      const s1 = dl >= 16 ? pool.substring(o1, o1 + (u32[db + 3] ?? 0)) : "";
      n.title = s1 === "" ? null : s1;
      return true;
    }
    case 16:
    {
      const dl = u32[w + W_DATA_LEN] ?? 0;
      const db = (wire.typeDataB + (u32[w + W_DATA_OFFSET] ?? 0)) >> 2;
      const o0 = u32[db] ?? 0;
      n.url = dl >= 8 ? pool.substring(o0, o0 + (u32[db + 1] ?? 0)) : "";
      const o1 = u32[db + 2] ?? 0;
      n.alt = dl >= 16 ? pool.substring(o1, o1 + (u32[db + 3] ?? 0)) : "";
      const o2 = u32[db + 4] ?? 0;
      const s2 = dl >= 24 ? pool.substring(o2, o2 + (u32[db + 5] ?? 0)) : "";
      n.title = s2 === "" ? null : s2;
      return true;
    }
    case 17:
    {
      const dl = u32[w + W_DATA_LEN] ?? 0;
      const db = (wire.typeDataB + (u32[w + W_DATA_OFFSET] ?? 0)) >> 2;
      const o0 = u32[db] ?? 0;
      n.identifier = dl >= 8 ? pool.substring(o0, o0 + (u32[db + 1] ?? 0)) : "";
      const o1 = u32[db + 2] ?? 0;
      n.label = dl >= 16 ? pool.substring(o1, o1 + (u32[db + 3] ?? 0)) : "";
      const b2 = dl > 16 ? (u8[(db << 2) + 16] ?? 0) : 0;
      n.referenceType = FUSED_ENUM_0[b2] ?? FUSED_ENUM_0[0];
      return true;
    }
    case 18:
    {
      const dl = u32[w + W_DATA_LEN] ?? 0;
      const db = (wire.typeDataB + (u32[w + W_DATA_OFFSET] ?? 0)) >> 2;
      const o0 = u32[db] ?? 0;
      n.identifier = dl >= 8 ? pool.substring(o0, o0 + (u32[db + 1] ?? 0)) : "";
      const o1 = u32[db + 2] ?? 0;
      n.label = dl >= 16 ? pool.substring(o1, o1 + (u32[db + 3] ?? 0)) : "";
      const b2 = dl > 16 ? (u8[(db << 2) + 16] ?? 0) : 0;
      n.referenceType = FUSED_ENUM_0[b2] ?? FUSED_ENUM_0[0];
      const o3 = u32[db + 5] ?? 0;
      n.alt = dl >= 28 ? pool.substring(o3, o3 + (u32[db + 6] ?? 0)) : "";
      return true;
    }
    case 19:
    {
      const dl = u32[w + W_DATA_LEN] ?? 0;
      const db = (wire.typeDataB + (u32[w + W_DATA_OFFSET] ?? 0)) >> 2;
      const o0 = u32[db] ?? 0;
      n.identifier = dl >= 8 ? pool.substring(o0, o0 + (u32[db + 1] ?? 0)) : "";
      const o1 = u32[db + 2] ?? 0;
      n.label = dl >= 16 ? pool.substring(o1, o1 + (u32[db + 3] ?? 0)) : "";
      return true;
    }
    case 20:
    {
      const dl = u32[w + W_DATA_LEN] ?? 0;
      const db = (wire.typeDataB + (u32[w + W_DATA_OFFSET] ?? 0)) >> 2;
      const o0 = u32[db] ?? 0;
      n.identifier = dl >= 8 ? pool.substring(o0, o0 + (u32[db + 1] ?? 0)) : "";
      const o1 = u32[db + 2] ?? 0;
      n.label = dl >= 16 ? pool.substring(o1, o1 + (u32[db + 3] ?? 0)) : "";
      return true;
    }
    case 27:
    {
      const dl = u32[w + W_DATA_LEN] ?? 0;
      const db = (wire.typeDataB + (u32[w + W_DATA_OFFSET] ?? 0)) >> 2;
      const o0 = u32[db] ?? 0;
      const s0 = dl >= 8 ? pool.substring(o0, o0 + (u32[db + 1] ?? 0)) : "";
      n.meta = s0 === "" ? null : s0;
      const o1 = u32[db + 2] ?? 0;
      n.value = dl >= 16 ? pool.substring(o1, o1 + (u32[db + 3] ?? 0)) : "";
      return true;
    }
    case 28:
    {
      const dl = u32[w + W_DATA_LEN] ?? 0;
      const db = (wire.typeDataB + (u32[w + W_DATA_OFFSET] ?? 0)) >> 2;
      const o0 = u32[db + 2] ?? 0;
      n.value = dl >= 16 ? pool.substring(o0, o0 + (u32[db + 3] ?? 0)) : "";
      return true;
    }
    case 102:
    case 103:
    case 104:
    {
      const dl = u32[w + W_DATA_LEN] ?? 0;
      const db = (wire.typeDataB + (u32[w + W_DATA_OFFSET] ?? 0)) >> 2;
      const o0 = u32[db] ?? 0;
      n.value = restorePhantomSpaces(dl >= 8 ? pool.substring(o0, o0 + (u32[db + 1] ?? 0)) : "");
      return true;
    }
    case 0:
    case 1:
    case 3:
    case 4:
    case 11:
    case 12:
    case 14:
    case 22:
    case 23:
    case 24:
    case 33:
    case 34:
    case 35:
    case 36:
      return true;
    default:
      return false;
  }
}

/** Install `position` (a zero start line marks a synthesized node) and the tag's fixed
 *  fields straight off the wire; `false` hands the tag to the caller's reader path. */
export function readHastWireNode(
  wire: ArenaWire,
  nodeId: number,
  nodeType: number,
  node: object,
): boolean {
  const { u8, u32, pool } = wire;
  const w = wire.nodesW + nodeId * wire.strideW;
  const wp = w + W_START_OFFSET;
  const startLine = u32[wp + 2] ?? 0;
  if (startLine !== 0) {
    (node as { position?: Position }).position = {
      start: { offset: u32[wp] ?? 0, line: startLine, column: u32[wp + 3] ?? 0 },
      end: { offset: u32[wp + 1] ?? 0, line: u32[wp + 4] ?? 0, column: u32[wp + 5] ?? 0 },
    };
  }
  const n = node as Record<string, unknown>;
  switch (nodeType) {
    case 2:
    case 3:
    case 5:
    case 13:
    {
      const dl = u32[w + W_DATA_LEN] ?? 0;
      const db = (wire.typeDataB + (u32[w + W_DATA_OFFSET] ?? 0)) >> 2;
      const o0 = u32[db] ?? 0;
      n.value = dl >= 8 ? pool.substring(o0, o0 + (u32[db + 1] ?? 0)) : "";
      return true;
    }
    case 12:
    case 14:
    {
      const dl = u32[w + W_DATA_LEN] ?? 0;
      const db = (wire.typeDataB + (u32[w + W_DATA_OFFSET] ?? 0)) >> 2;
      const o0 = u32[db] ?? 0;
      n.value = restorePhantomSpaces(dl >= 8 ? pool.substring(o0, o0 + (u32[db + 1] ?? 0)) : "");
      return true;
    }
    case 1:
    {
      const dl = u32[w + W_DATA_LEN] ?? 0;
      const db = (wire.typeDataB + (u32[w + W_DATA_OFFSET] ?? 0)) >> 2;
      let tagName = "";
      const properties: Record<string, HastProperty["value"]> = {};
      if (dl >= 16) {
        const toff = u32[db] ?? 0;
        tagName = pool.substring(toff, toff + (u32[db + 1] ?? 0));
        const count = u32[db + 2] ?? 0;
        for (let i = 0; i < count; i++) {
          const base = db + 4 + i * 5;
          const noff = u32[base] ?? 0;
          const voff = u32[base + 3] ?? 0;
          properties[pool.substring(noff, noff + (u32[base + 1] ?? 0))] = decodeElementProp(
            u8[(base << 2) + 8] ?? 0,
            pool.substring(voff, voff + (u32[base + 4] ?? 0)),
          );
        }
      }
      n.tagName = tagName;
      n.properties = properties;
      return true;
    }
    case 0:
    case 4:
      return true;
    default:
      return false;
  }
}
